# 并行 Vibe Coding 2.0：tmux 让四个 AI 在一个窗口里协同工作

> 当你不再需要在四个终端窗口间来回切换时，编程体验会有多爽？

---

## 从混乱的四个窗口说起

还记得上次的故事吗？我们用 Colyn 同时开了四个 Claude Code，四个终端窗口，四个开发服务器...

但问题来了：

- 💢 **窗口太多**：四个终端、四个浏览器，屏幕上密密麻麻
- 😵 **经常迷路**：这是哪个 worktree？哦这是 task-3...
- 🔄 **频繁切换**：Cmd+Tab、Cmd+Tab、Cmd+Tab...找到了！
- 📊 **难以监控**：想看某个 AI 在干什么？先找到对应的窗口

**能不能有更优雅的方式？**

答案是：**Colyn + tmux 集成**。

---

## 全新的工作流：一个窗口，四个世界

还是同样的任务——48小时开发任务管理 App。但这次，一切都在一个 tmux session 里。

### 第一步：初始化项目（自动创建 session）

```bash
cd my-task-app
colyn init -p 3000
```

之前的输出：
```
✓ 项目初始化完成
```

现在的输出：
```
✓ 项目初始化完成
✓ 检测到你不在 tmux 中
✓ 已创建 tmux session: my-task-app
✓ 已设置 Window 0: main
  ├─ Claude Code  (左侧 60%)
  ├─ Dev Server   (右上 12%)
  └─ Bash         (右下 28%)

💡 提示: 运行 'tmux attach -t my-task-app' 进入工作环境
```

**发生了什么？**

Colyn 检测到我不在 tmux 中，自动：
1. 创建了名为 `my-task-app` 的 tmux session
2. 创建了第一个 window（Window 0: main）
3. 自动分割成 3 个 pane
4. 每个 pane 都切换到了主分支目录

我执行 `tmux attach -t my-task-app`，进入了这个画面：

```
┌────────────────────────────────────────────────────────────┐
│ my-task-app:0 main*                                        │
├────────────────────────┬───────────────────────────────────┤
│                        │ my-task-app (main)                │
│                        │ $ npm run dev                     │
│ my-task-app (main)     │                                   │
│                        │ > next dev                        │
│ $ claude               │   ▲ Next.js 14.0.0                │
│                        │   - Local:   http://localhost:3000│
│                        │                                   │
│                        ├───────────────────────────────────┤
│                        │ my-task-app (main)                │
│                        │ $ colyn list                      │
│                        │                                   │
│                        │ ┌────────┬────────┬──────┬─────┐ │
│                        │ │ ID     │ Branch │ Port │ ... │ │
│                        │ │ 0-main │ main   │ 3000 │ ... │ │
│                        │ └────────┴────────┴──────┴─────┘ │
│                        │ $ _                               │
└────────────────────────┴───────────────────────────────────┘
```

**三个 pane 自动就位**：
- **左侧**（60%）：自动启动/继续 Claude 会话
- **右上**（12%）：开发服务器已经在运行（自动检测到 `npm run dev`）
- **右下**（28%）：普通的 bash，随时执行命令

### 第二步：创建四个并行分支（自动创建 window）

现在开始创建 worktree。我在右下的 bash pane 里执行：

```bash
colyn add feature/auth
```

**魔法发生了！**

屏幕顶部状态栏变化：
```
my-task-app:0 main  →  my-task-app:1 feature/auth*
```

Colyn 自动：
1. 创建了新的 worktree
2. 创建了新的 tmux window（Window 1）
3. 自动切换到这个 window
4. 自动分割成 3 个 pane
5. 每个 pane 都在 `worktrees/task-1` 目录
6. 右上的 pane 已经启动了 dev server（端口 3001）

我继续创建其他三个：

```bash
colyn add feature/tasks       # → Window 2 (PORT=3002)
colyn add feature/categories  # → Window 3 (PORT=3003)
colyn add feature/dashboard   # → Window 4 (PORT=3004)
```

现在我的 tmux 状态栏显示：
```
[my-task-app] 0:main  1:feature/auth*  2:feature/tasks  3:feature/categories  4:feature/dashboard
```

**一切都在一个 tmux session 里！**

### 第三步：四个 window，四个 Claude（优雅版）

现在开始最爽的部分。

**切换到 Window 1（Ctrl-b 1）**

```
┌────────────────────────────────────────────────────────────┐
│ my-task-app:1 feature/auth*                                │
├────────────────────────┬───────────────────────────────────┤
│                        │ task-1 (feature/auth)             │
│                        │ $ npm run dev                     │
│ task-1 (feature/auth)  │                                   │
│                        │ > next dev                        │
│ $ claude               │   ▲ Next.js 14.0.0                │
│                        │   - Local:   http://localhost:3001│
│ > 帮我实现用户认证功能 │                                   │
│   包括：               │                                   │
│   1. NextAuth.js       ├───────────────────────────────────┤
│   2. 邮箱密码登录      │ task-1 (feature/auth)             │
│   3. Google OAuth      │ $ git status                      │
│   4. 登录注册页面      │ On branch feature/auth            │
│                        │ ...                               │
└────────────────────────┴───────────────────────────────────┘
```

左侧 pane 已自动启动 Claude，会话直接开始。同时：
- **右上**：开发服务器已经在运行（3001 端口）
- **右下**：可以随时执行 git、测试等命令

**切换到 Window 2（Ctrl-b 2）**

```
┌────────────────────────────────────────────────────────────┐
│ my-task-app:2 feature/tasks*                               │
├────────────────────────┬───────────────────────────────────┤
│ task-2 (feature/tasks) │ task-2 (feature/tasks)            │
│                        │ $ npm run dev                     │
│ $ claude               │   ▲ Next.js 14.0.0                │
│                        │   - Local:   http://localhost:3002│
│ > 帮我实现任务管理功能 │                                   │
│   1. CRUD API          ├───────────────────────────────────┤
│   2. 任务列表页面      │ task-2 (feature/tasks)            │
│   3. 拖拽排序          │ $ npm test                        │
│   ...                  │ ...                               │
└────────────────────────┴───────────────────────────────────┘
```

同样的布局，不同的 worktree，不同的端口。

**依次启动 Window 3 和 Window 4**

四个 Claude Code 同时工作，但我只需要在一个 tmux session 里切换：
- **Ctrl-b 1** - 认证功能
- **Ctrl-b 2** - 任务管理
- **Ctrl-b 3** - 分类标签
- **Ctrl-b 4** - 仪表盘

不需要 Cmd+Tab！不需要找窗口！**一切尽在掌握。**

### 第四步：巡视进度（超快切换）

两个小时后，我想看看各个功能的进度。

在任意 pane 执行 `colyn list`：

```
┌────────┬─────────────────────┬──────┬──────┬──────────────────┐
│ ID     │ 分支                │ 端口 │ Diff │ 路径             │
├────────┼─────────────────────┼──────┼──────┼──────────────────┤
│ 0-main │ main                │ 3000 │ -    │ my-task-app      │
│ 1      │ feature/auth        │ 3001 │ +127 │ task-1           │
│ 2      │ feature/tasks       │ 3002 │ +89  │ task-2           │
│ 3      │ feature/categories  │ 3003 │ +56  │ task-3           │
│ 4      │ feature/dashboard   │ 3004 │ +134 │ task-4           │
└────────┴─────────────────────┴──────┴──────┴──────────────────┘
```

**注意 ID 列** - 直接显示 tmux window 编号！主分支显示为 `0-main`，worktree 直接显示数字 ID。

想看认证功能？`Ctrl-b 1` 秒切！
想看仪表盘？`Ctrl-b 4` 秒切！

每个 window 里：
- 左侧是 Claude 的对话历史
- 右上是实时的开发服务器日志
- 右下可以执行任何命令

**一目了然，行云流水。**

### 第五步：发现问题，快速调试

我在 Window 4 的右上 pane 看到了报错：

```
task-4 (feature/dashboard)
$ npm run dev

Error: Module not found 'recharts'
```

不需要切窗口，直接在右下的 bash pane：

```bash
npm install recharts
```

安装完成，右上的 dev server 自动重启，问题解决。

**整个过程不超过 10 秒，无需离开当前 window。**

### 第六步：验证功能（分屏浏览器）

我开启浏览器的分屏模式，一次性打开四个端口：

```
┌──────────────────┬──────────────────┐
│ localhost:3001   │ localhost:3002   │
│ [认证页面]        │ [任务列表]        │
├──────────────────┼──────────────────┤
│ localhost:3003   │ localhost:3004   │
│ [分类管理]        │ [数据仪表盘]      │
└──────────────────┴──────────────────┘
```

同时，tmux window 也可以分屏查看多个：

```bash
# 创建一个 5x5 的监控布局
tmux new-window -n monitor
tmux split-window -h
tmux split-window -h
tmux select-pane -t 0
tmux split-window -v
tmux select-pane -t 2
tmux split-window -v

# 每个 pane 显示一个 worktree 的 dev server 日志
```

现在我可以同时监控所有 AI 的工作进度！

### 第七步：合并代码（在任意 window）

功能验证完成，开始合并。我可以在任意 window 执行：

```bash
# 在 Window 0（main）执行
Ctrl-b 0

# 在右下的 bash pane
colyn merge 1 --push
```

Colyn 会自动处理一切，我在右上的 pane 看到实时输出：

```
✓ 正在合并 feature/auth → main...
✓ 测试通过
✓ 推送到远程
✓ 合并完成！
```

继续合并其他三个：

```bash
colyn merge 2 --push
colyn merge 3 --push
colyn merge 4 --push
```

所有功能合并完成！现在主分支已经包含了所有新功能，可以部署上线了。

---

## 对比：没有 tmux vs 有 tmux

### 传统方式（4 个独立终端）

```
Terminal 1          Terminal 2          Terminal 3          Terminal 4
┌────────────┐     ┌────────────┐     ┌────────────┐     ┌────────────┐
│ task-1     │     │ task-2     │     │ task-3     │     │ task-4     │
│            │     │            │     │            │     │            │
│ $ claude   │     │ $ claude   │     │ $ claude   │     │ $ claude   │
│            │     │            │     │            │     │            │
└────────────┘     └────────────┘     └────────────┘     └────────────┘

问题：
❌ Cmd+Tab 找窗口
❌ 记不清哪个是哪个
❌ 开发服务器日志在哪？新开一个终端？
❌ 想执行命令？又要新开一个终端？
❌ 删除 worktree 后，终端窗口还在...
```

### tmux 集成方式（1 个 session）

```
tmux session: my-task-app
┌──────────────────────────────────────────────────────────┐
│ [0:main] [1:auth*] [2:tasks] [3:categories] [4:dashboard]│
├──────────────────┬───────────────────────────────────────┤
│                  │ ← 开发服务器实时日志                   │
│                  │                                       │
│  ← Claude 对话   ├───────────────────────────────────────┤
│                  │ ← Bash 命令行                         │
│                  │                                       │
└──────────────────┴───────────────────────────────────────┘

优势：
✅ Ctrl-b 0~4 秒切
✅ 状态栏显示所有 window
✅ 三个 pane 各司其职
✅ 自动创建、自动关闭
✅ 一个窗口掌控全局
```

---

## tmux 集成的设计哲学

### 1. 零配置，全自动

```typescript
// 你只需要记住一个命令
colyn init

// Colyn 自动检测：
if (isInTmux()) {
  // 已在 tmux 中 → 使用当前 session
} else {
  // 不在 tmux 中 → 创建新 session
}
```

### 2. 智能映射

```
Worktree     →  tmux Window
─────────────────────────────
main         →  Window 0
task-1       →  Window 1
task-2       →  Window 2
...
```

一个 worktree 对应一个 window，切换 worktree 就是切换 window。

### 3. 固定布局，统一体验

每个 window 都是相同的布局：

```
┌──────────────┬─────────┐
│              │  Dev    │
│              │  Server │  ← 60% : 12%
│   Claude     ├─────────┤
│   Code       │         │
│              │  Bash   │  ← 60% : 28%
└──────────────┴─────────┘
```

不需要每次手动分割 pane，不需要调整大小。
**打开就能用，布局始终如一。**

### 4. 开发服务器自动启动

```typescript
// Colyn 检测 package.json
{
  "scripts": {
    "dev": "next dev"  // ← 找到了！
  }
}

// 自动在右上 pane 运行
// PORT 环境变量已在 .env.local 中配置
npm run dev
```

你只需要在左侧和 Claude 对话，右上角的服务器已经在跑了。

---

## 实现细节揭秘

你可能好奇：Colyn 是怎么做到的？

### 检测 tmux 环境

```typescript
function isInTmux(): boolean {
  return !!process.env.TMUX;
}

function getCurrentSession(): string | null {
  const tmux = process.env.TMUX;
  if (!tmux) return null;

  // TMUX 格式：/path/to/socket,session_id,pane_id
  const sessionId = tmux.split(',')[1];

  // 获取 session 名称
  const result = execSync(`tmux display-message -p '#S'`).toString().trim();
  return result;
}
```

### 自动创建 window

```typescript
async function createWorktreeWindow(
  worktreePath: string,
  branch: string
) {
  const windowName = branch.replace('feature/', '');

  // 1. 创建新 window
  execSync(`tmux new-window -n "${windowName}" -c "${worktreePath}"`);

  // 2. 分割 pane（60% : 40%）
  execSync(`tmux split-window -h -p 40 -c "${worktreePath}"`);

  // 3. 分割右侧为上下两部分（上30%，下70%）
  execSync(`tmux split-window -v -p 70 -c "${worktreePath}"`);

  // 4. 在右上 pane 启动 dev server
  // PORT 环境变量已经在 .env.local 中配置好了
  execSync(`tmux send-keys -t 1 "npm run dev" Enter`);

  // 5. 自动启动/继续 Claude 会话（存在会话则 claude -c）
  const claudeCmd = hasClaudeSession(worktreePath) ? 'claude -c' : 'claude';
  execSync(`tmux send-keys -t 0 "${claudeCmd}" Enter`);
  execSync(`tmux select-pane -t 0`);
}
```

### 智能端口分配

```typescript
// Colyn 会在创建 worktree 时自动配置 .env.local
// 每个 worktree 有独立的端口号
// .env.local:
PORT=3001  # task-1
PORT=3002  # task-2
PORT=3003  # task-3

// dev server 会自动读取 PORT 环境变量
// 无需在命令行中手动指定端口
```

---

## 总结

**Colyn + tmux = 并行 Vibe Coding 的完美形态**

### 之前（Colyn 1.0）
- ✅ 多个 AI 并行工作
- ✅ Git worktree 隔离
- ⚠️ 但需要管理多个终端窗口

### 现在（Colyn 2.0 with tmux）
- ✅ 多个 AI 并行工作
- ✅ Git worktree 隔离
- ✅ **一个 tmux session 统一管理**
- ✅ **三个 pane 自动布局**
- ✅ **window 自动创建/关闭**
- ✅ **开发服务器自动启动**
- ✅ **秒切 worktree**

效率提升：
- 开发时间：**4 倍**（4 个 AI 并行）
- 窗口切换：**10 倍快**（Ctrl-b 1-4 vs Cmd+Tab 找窗口）
- 心智负担：**降低 90%**（不用记哪个窗口是什么）

**这不是锦上添花，这是质的飞跃。**

试试看，让你的 AI 军团在一个 tmux session 里和谐共舞吧！

---

*如果你觉得这个功能有用，欢迎在 [GitHub](https://github.com/colin-han/colyn) 上给个 Star ⭐*
